export default (theme) =>{
    return{
         container:
         {
         width: '500px',
         margin: '0 auto',
         padding : theme.spacing.unit * 2
         },
         textField: {
             marginBottom: theme.spacing.unit * 2
         }
     }
 }